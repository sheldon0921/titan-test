dev:
  base_url: "https://httpbin.org"
  timeout: 10
  # 🔐 账号信息 (避免硬编码在代码里)
  auth:
    username: "admin"
    password: "123"
    # 👇 新增：在这里配置你的长效 Token (如果配置了它，代码会自动跳过登录接口)
    token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.CONFIGURED_IN_YAML.123456"

  # 🛢️ 数据库配置 (示例)
  db:
    host: "127.0.0.1"
    port: 3306
    user: "root"
    password: "password"
    name: "test_db"

prod:
  base_url: "https://www.baidu.com"
  timeout: 10
  auth:
    username: "prod_admin"
    password: "secure_password"
    # 生产环境也可以配不同的 Token
    token: ""
  db:
    host: "192.168.1.100"
    port: 3306
    user: "admin"
    password: "secure_password"
    name: "prod_db"