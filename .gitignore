# .gitignore

# Python 编译文件
__pycache__/
*.py[cod]
*$py.class

# Pytest 缓存
.pytest_cache/

# 这里的 reports 我们不传，因为 CI 会自己生成
reports/
allure-results/

# 环境变量配置 (为了安全，通常不传带有真实密码的配置，这里视情况而定)
# 如果是只有你自己用的私有项目，为了方便可以先不忽略
# config/env.yaml

# Mac 系统文件
.DS_Store

# IDE 配置
.idea/
.vscode/