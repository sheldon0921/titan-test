dev:
  base_url: "https://httpbin.org"
  timeout: 10
  # 🔐 账号信息 (避免硬编码在代码里)
  auth:
    username: "admin"
    password: "123"
  # 🛢️ 数据库配置 (示例)
  db:
    host: "127.0.0.1"
    port: 3306
    user: "root"
    password: "password"
    name: "test_db"

prod:
  base_url: "https://www.baidu.com"
  timeout: 10
  auth:
    username: "prod_admin"
    password: "secure_password"
  db:
    host: "192.168.1.100"
    port: 3306
    user: "admin"
    password: "secure_password"
    name: "prod_db"